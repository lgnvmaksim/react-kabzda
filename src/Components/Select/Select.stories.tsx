import React from 'react';

type SelectPropsType = {
    value:any
    onChange: (value:any)=>void
}

const SelectStories = (props:SelectPropsType) => {
    return (
        <div>

        </div>
    );
};

export default SelectStories;